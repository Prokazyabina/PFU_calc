<!-- res/drawable/button_background_pressed.xml -->
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Цвет фона при нажатии -->
    <solid android:color="#1A5A6D" />

    <!-- Скругление углов (2 пикселя) -->
    <corners android:radius="2dp" />

    <!-- Обводка (необязательно) -->
    <stroke
            android:width="1dp"
            android:color="#24778D" />
</shape>